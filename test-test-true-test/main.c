//
//  main.c
//  test-test-true-test
//
//  Created by murata on 2022/10/02.
//

#include <stdio.h>

int main(int argc, const char * argv[]) {
  // insert code here...
  printf("Hello, World!  murata\n　　commit+push!");
  return 0;
  // 動けうごけ1SSHSSHSSHSSH SSHSSHSSSSSHHHHHHHHHHHHHHHHHHH
  // 家からだったらあっさりpushできたぞ！これがその証拠だ
  // 家からpush
  // 家からpush 10/3 8:24
  // これは研究室からpush 10/3 8:55]
  // from lab この行が最後ならね
  // all-tokenn のはず
  // はい，これがlabからコマンドラインでのpushです
  // これも，labからコマンドラインでpushです．
  // koremo です．git config --unset --global url."https://".insteadOf git://  OK　コマンドラインからcommit できた（Xcodeではだめ）
  // http,https proxy設定 git --unset で消した
  // xcodebuild -resolvePackageDependencies -scmProvider system やってみたのがこれ．まずはxcodeからはだめ．で，proxy設定すると．
  // 再度．再度．再度ですよ．
  //　commit
//  hogehoge
//  jlkajsd
//  fkaldsjfla
//  fjlakds
}
